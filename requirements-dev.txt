# Development and code quality tools — not installed in production
# Install with: pip install -r requirements-dev.txt

# Linting and formatting
ruff>=0.4.0

# Type checking
mypy>=1.9.0

# Security scanning
bandit>=1.7.7
safety>=3.0.0

# Complexity analysis
radon>=6.0.1
xenon>=0.9.1

# Pre-commit hooks
pre-commit>=3.7.0

# Testing extras (base test deps are in requirements.txt)
pytest>=8.0.0
pytest-asyncio>=0.21.0
pytest-cov>=4.0.0
httpx>=0.27.0
